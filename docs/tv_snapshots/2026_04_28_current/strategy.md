# namespace `strategy`

strategy(id, overlay, max_bars_back, max_lines_count)
strategy.entry(id, direction, qty, limit, stop, comment, alert_message, oca_type, comment_profit, comment_loss, when, alert_profit, alert_loss)
