# namespace `strategy`

strategy(id, overlay)
strategy.entry(id, direction, qty, limit, stop, comment, alert_message, oca_type, comment_profit, comment_loss, when)
ta.oldfunc(source) → series float
some text with incomplete function signature (
