//@version=6
strategy("v23 strategy seed 289", overlay = true)
qty = input.float(1.0, "Qty")
fast = ta.sma(close, 10)
slow = ta.sma(close, 20)
if ta.crossover(fast, slow)
    strategy.entry("L", strategy.long, qty = qty)
if ta.crossunder(fast, slow)
    strategy.close("L")
